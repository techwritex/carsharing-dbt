version: 2

models:
  - name: fct_payments
    columns:
      - name: payment_id
        tests:
          - unique
          - not_null
      - name: amount
        tests:
          - check_positive_values
